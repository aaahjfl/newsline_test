# newsdata/

`newsdata/` 当前保留为历史数据目录，主要承载已有原始数据集与历史采集结果。

当前阶段定义：
- 可视为“历史输入数据 / 历史采集结果保留区”
- 不是新架构约定的正式输出目录
- 后续若新架构形成稳定主流程，正式中间产物和结果应优先进入 `outputs/`

当前边界：
- 允许继续保留现有 `.json` 数据文件
- 不建议把新的正式运行结果继续默认写入这里
